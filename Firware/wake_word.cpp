#include <Arduino.h>
#include "esp_sr_iface.h"
#include "esp_sr_models.h"

bool detectWakeWord(const int16_t* audioData, size_t len) {
  // Placeholder logic — integrate ESP-SR here
  // e.g., detect "Jarvis" wake word
  return false; 
}
